#!/bin/bash
# Agent 27 — Inscription cron wrapper (automation intentionally disabled)

echo "Automation disabled: run inscription manually via dashboard phases."
echo "Use 'compose' first, then 'inscribe' after operator review."
exit 0
